# Parallel execution is enabled engine-wide, but the default stays same_thread so only tests
# annotated @Execution(CONCURRENT) run in parallel. A fixed pool guarantees real overlap
# regardless of the host core count.
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=same_thread
junit.jupiter.execution.parallel.mode.classes.default=same_thread
junit.jupiter.execution.parallel.config.strategy=fixed
junit.jupiter.execution.parallel.config.fixed.parallelism=4
