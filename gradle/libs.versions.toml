[versions]
tokens-sdk = "1.3.2"
junit-jupiter = "5.14.0"
assertj = "3.27.6"
artifactory = "6.0.2"

[libraries]
tokens-contracts = { module = "com.r3.corda.lib.tokens:tokens-contracts", version.ref = "tokens-sdk" }
tokens-workflows = { module = "com.r3.corda.lib.tokens:tokens-workflows", version.ref = "tokens-sdk" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit-jupiter" }
assertj-core = { module = "org.assertj:assertj-core", version.ref = "assertj" }
artifactory = { module = "com.jfrog.artifactory:com.jfrog.artifactory.gradle.plugin", version.ref = "artifactory" }

[plugins]
# Use the same minor version of Kotlin as Corda
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version = "1.9.25" }
